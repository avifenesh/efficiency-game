[package]
name = "solution"
version = "0.1.0"
edition = "2021"

# No external dependencies - standard library only
[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
debug = false
strip = true
panic = "abort"
